# locker-system


### If running locally

Use 'localhost' in the `MONGO_HOST` env variable, if running the app outside docker containers

### Tools
-Node
-Express
-Express-validator
-Mongodb
-Mongoose
-Mongo-express